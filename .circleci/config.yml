# Use the latest 2.1 version of CircleCI pipeline process engine.
# See: https://circleci.com/docs/2.0/configuration-reference
version: 2.1

# Define a job to be invoked later in a workflow.
# See: https://circleci.com/docs/2.0/configuration-reference/#jobs
jobs:
  run-current-branch:
    # Specify the execution environment. You can specify an image from Dockerhub or use one of our Convenience Images from CircleCI's Developer Hub.
    # See: https://circleci.com/docs/2.0/configuration-reference/#docker-machine-macos-windows-executor
    docker:
      - image: cimg/base:stable
    resource_class: small
    # Seqera workspace + compute environment are sourced from the CODEBASE (this file),
    # per-branch, NOT from shared CircleCI project env vars. Each commit therefore launches
    # on the compute environment that matches its own pipeline version, and a merge carries
    # the right CE with it (no manual CircleCI env-var flip). Only the SECRET TOWER_AUTH_TOKEN
    # remains a CircleCI project env var. The Nextflow version is likewise pinned in the launch
    # JSON below (preRunScript), so version + CE + workspace all travel with the commit.
    # These two IDs are deliberately in this PUBLIC repo: they are opaque identifiers, inert
    # without TOWER_AUTH_TOKEN, and must travel with the commit for the above to work. Do not
    # "fix" them into CircleCI env vars.
    environment:
      TOWER_WORKSPACE_ID: "40027684065767"
      TOWER_COMPUTE_ENV_ID: "ed0zjbIeIvKoUPuGsy8fA"
    # Add steps to the job
    # See: https://circleci.com/docs/2.0/configuration-reference/#steps
    steps:
      - checkout
      - run:
          name: Create the NAME environment variable
          command: |
            echo "BASH_ENV is $BASH_ENV"
            NAME=$(echo CI_$(echo $CIRCLE_SHA1 | cut -c -7)_$(date +"%H_%M_%S"))
            echo "NAME is $NAME"
            echo "export NAME=$NAME" >> "$BASH_ENV"
            echo "BASH_ENV is $BASH_ENV"
      - run:
          name: Create a new endpoint for the current branch
          command: |
            myvar=`curl -X POST "https://api.tower.nf/actions?workspaceId=$TOWER_WORKSPACE_ID" \
            -H "Accept: application/json" \
            -H "Authorization: Bearer $TOWER_AUTH_TOKEN" \
            -H "Content-Type: application/json" \
            -d '{"name":"'"$NAME"'","source":"tower","launch":{"configProfiles":["test"], "computeEnvId":"'"$TOWER_COMPUTE_ENV_ID"'","pipeline":"https://github.com/csgenetics/csgenetics_scrnaseq","workDir":"s3://csg-tower-bucket","revision":"'"$CIRCLE_SHA1"'","pullLatest":true,"preRunScript":"export NXF_VER=26.04.1"}}'`
            id=`echo $myvar | awk -F: {'print $2'} | sed 's/}//g'`
            echo "export ACTION_ID=$id" >> $BASH_ENV
            echo $id
      - run:
          name: Start the pipeline using the newly created endpoint
          command: |
            echo "NAME is $NAME"
            SHA=$NAME
            echo $NAME
            outdir="s3://csg-nextflow/circleci/"$NAME
            echo $outdir
            myvar=`curl -X POST "https://api.tower.nf/actions/$ACTION_ID/launch?workspaceId=$TOWER_WORKSPACE_ID" \
            -H "Authorization: Bearer $TOWER_AUTH_TOKEN" \
            -H "Content-Type: application/json" \
            -d '{"params":{"outdir":"'"$outdir"'"}}'`
            WORKFLOW_ID=`echo $myvar | awk -F: '{print $2}' | sed 's/}//g' | sed 's/"//g'`
            echo $WORKFLOW_ID
            echo "export WORKFLOW_ID=$WORKFLOW_ID" >> $BASH_ENV
      - run:
          name: Monitor pipeline execution
          command: |
            status=`curl -X GET "https://api.tower.nf/workflow/$WORKFLOW_ID?workspaceId=$TOWER_WORKSPACE_ID" \
            -H "Authorization: Bearer $TOWER_AUTH_TOKEN" \
            -H "Content-Type: application/json" | jq '.workflow.status' | sed 's/"//g'`
            echo $status
            while [[ "$status" != "SUCCEEDED" ]]; do
              if [ "$status" = "CANCELLED" ]; then
                echo "run was cancelled, deleting endpoint"
                curl -X DELETE "https://api.tower.nf/actions/$ACTION_ID?workspaceId=$TOWER_WORKSPACE_ID" \
                -H "Authorization: Bearer $TOWER_AUTH_TOKEN" \
                -H "Accept: application/json"
                exit 1
              fi
              if [ "$status" = "FAILED" ]; then
                echo "run failed"
                curl -X DELETE "https://api.tower.nf/actions/$ACTION_ID?workspaceId=$TOWER_WORKSPACE_ID" \
                -H "Authorization: Bearer $TOWER_AUTH_TOKEN" \
                -H "Accept: application/json"
                exit 1
              fi
              sleep 60
              status=`curl -X GET "https://api.tower.nf/workflow/$WORKFLOW_ID?workspaceId=$TOWER_WORKSPACE_ID" \
              -H "Authorization: Bearer $TOWER_AUTH_TOKEN" \
              -H "Content-Type: application/json" | jq '.workflow.status' | sed 's/"//g'`
            done
      - run:
          name: Delete the newly created endpoint
          command: |
            curl -X DELETE "https://api.tower.nf/actions/$ACTION_ID?workspaceId=$TOWER_WORKSPACE_ID" \
            -H "Authorization: Bearer $TOWER_AUTH_TOKEN" \
            -H "Accept: application/json"

  # Fast structural test: runs the whole pipeline in -stub mode (no real data,
  # no AWS credentials needed - the S3 download processes are stubbed), plus the
  # per-process module tests. Catches wiring / output-declaration regressions.
  nf-test:
    machine:
      image: ubuntu-2204:current
    resource_class: large
    steps:
      - checkout
      - run:
          name: Install Java 17, Nextflow, nf-test
          command: |
            sudo apt-get update -y && sudo apt-get install -y openjdk-17-jre-headless
            curl -s https://get.nextflow.io | bash && sudo mv nextflow /usr/local/bin/
            curl -fsSL https://get.nf-test.com | bash && sudo mv nf-test /usr/local/bin/
      - run:
          name: nf-test (whole-pipeline -stub DAG test + module tests)
          command: |
            export NXF_VER=26.04.1
            nf-test test tests/nf/pipeline_stub.nf.test modules/local/save_resolved_configuration/tests/main.nf.test --verbose

  # This repo is PUBLIC. Fails the build if agent working files, ad-hoc internal sample
  # sheets, credentials or beast-local paths get committed. See CLAUDE.md for the parts
  # that cannot be mechanised. Deliberately dependency-free so it is always cheap to run.
  check-no-internal-files:
    docker:
      - image: cimg/base:stable
    resource_class: small
    steps:
      - checkout
      - run:
          name: No internal-only files, credentials or local paths
          command: tests/check_no_internal_files.sh

  # Headless-browser smoke test for the consolidated HTML report. Generates a
  # report from a committed fixture and renders it in Chromium to assert it is
  # FUNCTIONAL (no JS console errors, Plotly defined, every plot drew an SVG, the
  # sample dropdown selects the right pane and does not collapse, print reveals all
  # panes, the CSV stays separator-free). Catches the blank-plot / dropdown-collapse
  # class of regression that file-level checks cannot see. Also runs the report
  # number-formatting unit tests. Deliberately dependency-light: the report
  # generator and these tests need only jinja2 + plotly, so this avoids the heavy
  # single-cell pipeline Python stack (anndata/scipy/etc.).
  report-smoke:
    docker:
      - image: cimg/python:3.12
    resource_class: medium
    steps:
      - checkout
      - run:
          name: Install report deps + headless Chromium
          command: |
            pip install --quiet jinja2 plotly pytest playwright
            python -m playwright install --with-deps chromium
      - run:
          name: Report smoke + number-formatting tests
          command: |
            python -m pytest tests/python/test_report_smoke.py tests/python/test_number_formatting.py -v


# Invoke jobs via workflows
# See: https://circleci.com/docs/2.0/configuration-reference/#workflows
# NOTE: the run-current-branch (Seqera Tower) job needs exactly ONE CircleCI project env
# var: TOWER_AUTH_TOKEN (the Seqera access token - a secret). The workspace and compute-env
# IDs are codebase-controlled in the job's `environment:` block above (per-branch), so no
# CircleCI env var needs flipping when branches on different pipeline versions coexist.
workflows:
  ci:
    jobs:
      - check-no-internal-files
      - nf-test
      - report-smoke
      - run-current-branch
